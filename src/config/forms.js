/**
 * Form Endpoints Configuration
 * 
 * North & Finch is a frontend-only project. Form submissions are submitted
 * directly to external third-party form services (e.g. MyFormConnect, Formspree, Basin).
 * 
 * Replace these placeholder strings with your actual production endpoints.
 */
export const formEndpoints = {
  contact: "YOUR_CONTACT_FORM_ENDPOINT",
  newsletter: "YOUR_NEWSLETTER_FORM_ENDPOINT",
  careers: "YOUR_CAREERS_FORM_ENDPOINT",
  keepInformed: "YOUR_KEEP_INFORMED_FORM_ENDPOINT"
};
